name: "3940-Lab-3"
services:
  web:
    container_name: frontend
    build: /frontend
    ports:
      - "3000:80"
  api:
    container_name: backend
    build: /backend
    ports:
      - "5000:5000"
    environment:
      - ASPNETCORE_HTTP_PORTS=5000